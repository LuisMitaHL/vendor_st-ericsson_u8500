In order to build the JNI shared objects JAVA_HOME environment variable
must be set in order for the makefiles to locate correct jni.h and
jni_md.h

The install target of the Makefile will install Flash Tool Backend in
$(FLASH_KIT_TARGET_DIR)/flash-tool-backend
