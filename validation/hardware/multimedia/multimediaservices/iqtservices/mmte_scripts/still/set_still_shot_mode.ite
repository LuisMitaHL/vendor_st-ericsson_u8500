#set shot mode
#alias STILL_CONTINUOUS_MODE		0
#alias STILL_SINGLE_MODE		1
#alias STILL_BURST_MODE			2

alias MODE $1

if MODE == 0
	alias STILL_MODE  STILL_CONTINUOUS_MODE
endif

if MODE == 1
	alias STILL_MODE  STILL_SINGLE_MODE
endif
	
if  MODE == 2	
alias STILL_MODE  STILL_BURST_MODE
endif