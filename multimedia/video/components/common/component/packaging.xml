<?xml version="1.0" encoding="UTF-8"?>

<packages>
  <fileset id="always-delivered">
    <include name="component/**" />
    <include name="Make*" />
  </fileset>

  <package name="arm-proxy-bin">
    <fileset idref="always-delivered" />
    <fileset>
	<include name="memconfig/**" />
	<include name="splitrom/**" />
	<include name="src/**" />
	<include name="inc/**" />
	<include name="update_*" />    
      
    </fileset>
  </package>

  <package name="arm-proxy-src">
    <fileset idref="always-delivered" />
    <fileset>
    	<include name="memconfig/**" />
    	<include name="splitrom/**" />
    	<include name="src/**" />
    	<include name="inc/**" />
    	<include name="update_*" />    
    </fileset>
  </package>

  <package name="arm-nmf-bin">
    <fileset idref="always-delivered" />
    <fileset>
      <include name="nmf/**" /> 
    </fileset>
  </package>
  <package name="arm-nmf-src">
    <fileset idref="always-delivered" />
    <fileset>
      <include name="nmf/**" />
    </fileset>
  </package>
</packages>