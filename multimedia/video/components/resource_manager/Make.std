# -*- Mode: Makefile -*-
# vim:syntax=make:

# install need including Make.std.config and Make.std.rules
include $(MMROOT)/video/makefiles/Make.std.config

INSTALL_IDT = t1xhv_resource_manager.idt
EXTRA_INSTALL += install-idt

todo:

include $(MMROOT)/video/makefiles/Make.std.rules

