#include <ext_grab_types.idt>

/********************************************
 * reports errors to upper layers. Mostly errors coming from grab component
 ********************************************/
void error(enum e_grabError error_id, t_uint16 data, enum e_grabPipeID pipe_id);