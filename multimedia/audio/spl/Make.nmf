# -*- # -*- Mode: Makefile -*-
# vim:syntax=make:
include $(MM_MAKEFILES_DIR)/AudioNmfConfig.mk
CPPFLAGS+=-DNVERBOSE

USER2MPCITFS_CPP=spl.nmfil.effect.configure \
                 # \
                 spl.nmfil.effect.construct



COMPONENTS= spl.nmfil.effect

include $(MM_MAKEFILES_DIR)/SharedNmfRules.mk
