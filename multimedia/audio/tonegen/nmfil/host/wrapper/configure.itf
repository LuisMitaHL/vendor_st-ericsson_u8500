#include <samplefreq.idt>

typedef struct {
  t_sample_freq  freq;
  t_uint16       nof_channels;
  t_uint16       nof_bits_per_sample;
} t_tonegen_wrapper_config;

void setParameter(t_tonegen_wrapper_config config);
