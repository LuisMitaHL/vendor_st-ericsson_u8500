#include <libeffects/mpc/libdBconv/include/dBconv_include.h>

interface libeffects.mpc.libdBconv.libdBconv unmanaged {
    MMshort get_gain_from_dB(MMshort gaindB, MMshort *shift_res);
    MMshort get_dB_from_gain(MMshort gainl, MMshort shift);
}

