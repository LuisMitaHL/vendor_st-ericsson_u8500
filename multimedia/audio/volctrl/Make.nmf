# -*- # -*- Mode: Makefile -*-
# vim:syntax=make:
include $(MM_MAKEFILES_DIR)/AudioNmfConfig.mk
CPPFLAGS+=-DNVERBOSE

USER2MPCITFS_CPP=volctrl.nmfil.effect.configure

COMPONENTS= volctrl.nmfil.effect

include $(MM_MAKEFILES_DIR)/SharedNmfRules.mk
