This directory only contains tests that actually test the speech proc wrapper and NOT the processing library.
Thus all test use speech processing library in bypass mode.
Tests specific to the processing library should be put in <speech_proc_library>/mmte_script directory
