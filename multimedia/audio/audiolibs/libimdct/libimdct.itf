#include <audiolibs/common/include/imdct.h>

interface audiolibs.libimdct.libimdct unmanaged {
	void imdct_compress_ramxy(Float *Spectra,
							 Float *Samples,
							 Float YMEM * scratch_y,
					 		int ln);
	void imdct_ramxy(Float *Spectra,
					Float *Samples,
					Float YMEM * scratch_y,
					int ln);
}