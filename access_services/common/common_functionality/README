This module contains telephony related utility functions that are shared among the access_services modules.

Static libraries are used when security is a concern. Otherwize dynamic libraries are the default.
