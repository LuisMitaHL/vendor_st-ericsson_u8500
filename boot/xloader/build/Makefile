include config.mk

ifdef BUILD_8500B0
DIRS += $(wildcard 8500b0_xloader)
BOOT_IMAGES += 8500b0
endif

ifdef BUILD_SNOWBALL_A02
DIRS += $(wildcard snowball_a02_xloader)
BOOT_IMAGES += snowball_a02
endif

ifdef BUILD_9540A0
DIRS += $(wildcard 9540a0_xloader)
BOOT_IMAGES += 9540a0
endif

ifdef BUILD_8540A0
DIRS += $(wildcard 8540a0_xloader)
BOOT_IMAGES += 8540a0
endif

include build_common.mk
